import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Contribute a Prompt | PromptVault',
  description: 'Share your AI prompts with the project management community. First contribution is free!',
}

export default function ContributeLayout({ children }: { children: React.ReactNode }) {
  return children
}
